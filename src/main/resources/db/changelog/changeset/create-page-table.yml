databaseChangeLog:
  - changeSet:
      id: create-page
      author: halcyon
      changes:
        - createTable:
            tableName: pages
            columns:
              - column:
                  name: id
                  type: bigint
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false

              - column:
                  name: name
                  type: varchar(255)
                  constraints:
                    nullable: false

              - column:
                  name: name
                  type: varchar(255)
                  constraints:
                    nullable: false

              - column:
                  name: course_id
                  type: bigint
                  constraints:
                    foreignKeyName: course
                    referencedTableName: courses
                    referencedColumnNames: id